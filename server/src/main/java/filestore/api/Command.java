package filestore.api;

import io.nure.filestore.web.FileHubWebApplication;

/**
 * Abstract base for intention of client to change state of FileHub application
 * {@link FileHubWebApplication} during some process {@link ApplicationProcess}.
 */
public interface Command {
}
