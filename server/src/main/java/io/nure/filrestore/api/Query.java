package io.nure.filrestore.api;

import io.nure.filestore.web.FileStoreWebApplication;

/**
 * An abstract base for intention of a client to read state of {@link FileStoreWebApplication}.
 */
public interface Query {
}
