package io.nure.filrestore.api;

import io.nure.filestore.web.FileStoreWebApplication;

/**
 * Abstract base for intention of client to change state of FileHub application
 * {@link FileStoreWebApplication} during some process {@link ApplicationProcess}.
 */
public interface Command {
}
